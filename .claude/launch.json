{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "imweb-preview",
      "runtimeExecutable": "npx",
      "runtimeArgs": [
        "vite",
        "preview"
      ],
      "port": 4173
    },
    {
      "name": "imweb-preview-alt",
      "runtimeExecutable": "npx",
      "runtimeArgs": [
        "vite",
        "preview",
        "--port",
        "4174",
        "--strictPort"
      ],
      "port": 4174
    }
  ],
  "_comment": [
    "Preview servers ONLY. Do not add a dev-server entry here.",
    "",
    "`.claude/hooks/guard-dev-server.sh` blocks a second Vite dev server on any",
    "port but the owner's 5173, because a different port is a different origin:",
    "API keys, GLSL user presets and warp slots saved on 5173 read as missing,",
    "and that looks like an app bug rather than like two servers. See LEARNED.md",
    "2026-07-12, which cost a session to a 'test-connection paradox' that was",
    "config split across two ports.",
    "",
    "An entry here would route around that hook, which only sees Bash — the",
    "preview_start tool does not go through it. `vite preview` is the sanctioned",
    "path and is isolated from the owner's session anyway. It costs a rebuild",
    "per change; the stale-bundle trap that comes with hashed dist filenames is",
    "written up in the verify skill."
  ]
}